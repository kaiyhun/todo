/**
 * Rendered for the `@modal` slot whenever no intercepting route matches — for
 * example on a hard load of /board. Returning null keeps the slot empty.
 */
export default function ModalDefault() {
  return null;
}
